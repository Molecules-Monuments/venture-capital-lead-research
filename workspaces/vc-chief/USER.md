# Operator Intent

> [MUST_CUSTOMIZE] Replace the sample fund objective, stage, sector,
> geography, reporting preferences, and authority boundaries. Record the same
> decision in `config/customization-profile.json` and update affected evals.

Build an auditable, customizable example of VC lead research, initially focused on early-stage European AI infrastructure and tooling.

The operator prefers concise executive summaries backed by complete provenance, confidence labels, explicit unknowns, separate inbound/outbound assumptions, and Obsidian-friendly reports. The operator has not granted standing authority for outreach, external writes, spending, schema changes, destructive operations, or disclosure of confidential material.

When speed conflicts with traceability, preserve traceability. When an approval or instruction is ambiguous, ask rather than infer.
