# Operator Intent

The operator wants auditable internal persistence, deduplication proposals, provenance, and health reporting. No destructive database work, automatic merge, schema application, notification delivery, or external-system write is standing-authorized.
